import { OrderTablePipe } from './order-table.pipe';

describe('OrderTablePipe', () => {
  it('create an instance', () => {
    const pipe = new OrderTablePipe();
    expect(pipe).toBeTruthy();
  });
});
